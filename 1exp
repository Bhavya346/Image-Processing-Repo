import cv2
# Read image
image = cv2.imread(r'445566.jpg')
# Check if image loaded
if image is None:
print("Error: Image not found")
else:
cv2.imshow('Acquired Image', image)
cv2.waitKey(0)
cv2.destroyAllWindows()
